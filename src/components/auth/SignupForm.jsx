import React from "react";

function SignupForm() {
  return <form>
    
  </form>;
}

const CustomInput = ({ name, id }) => {
  return (
    <div>
      <label>{name}</label>
      <input type="type" />
    </div>
  );
};

export default SignupForm;
